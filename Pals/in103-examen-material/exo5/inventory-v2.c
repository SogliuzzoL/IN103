#include <stdlib.h>
#include <stdio.h>


int main (void) {

  int size = 0;
  fscanf (stdin, "%d", &size);


  for (int i = 0; i < size; i++) {
    int x;
    fscanf (stdin, "%d", &x);

    /* A COMPLETER */
  }

  int nbRequest = 0;
  fscanf (stdin, "%d", &nbRequest);

  for (int j = 0; j < nbRequest; j++) {
    int x = 0;
    fscanf (stdin, "%d", &x);

    /* A COMPLETER */
  }

  /* A COMPLETER */

  return 0;
}
